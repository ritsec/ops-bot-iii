// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ritsec/ops-bot-iii/ent/openstack"
	"github.com/ritsec/ops-bot-iii/ent/user"
)

// Openstack is the model entity for the Openstack schema.
type Openstack struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Time of last reset
	Timestamp time.Time `json:"timestamp,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the OpenstackQuery when eager-loading is set.
	Edges          OpenstackEdges `json:"edges"`
	user_openstack *string
	selectValues   sql.SelectValues
}

// OpenstackEdges holds the relations/edges for other nodes in the graph.
type OpenstackEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OpenstackEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Openstack) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case openstack.FieldID:
			values[i] = new(sql.NullInt64)
		case openstack.FieldTimestamp:
			values[i] = new(sql.NullTime)
		case openstack.ForeignKeys[0]: // user_openstack
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Openstack fields.
func (o *Openstack) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case openstack.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			o.ID = int(value.Int64)
		case openstack.FieldTimestamp:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field timestamp", values[i])
			} else if value.Valid {
				o.Timestamp = value.Time
			}
		case openstack.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_openstack", values[i])
			} else if value.Valid {
				o.user_openstack = new(string)
				*o.user_openstack = value.String
			}
		default:
			o.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Openstack.
// This includes values selected through modifiers, order, etc.
func (o *Openstack) Value(name string) (ent.Value, error) {
	return o.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Openstack entity.
func (o *Openstack) QueryUser() *UserQuery {
	return NewOpenstackClient(o.config).QueryUser(o)
}

// Update returns a builder for updating this Openstack.
// Note that you need to call Openstack.Unwrap() before calling this method if this Openstack
// was returned from a transaction, and the transaction was committed or rolled back.
func (o *Openstack) Update() *OpenstackUpdateOne {
	return NewOpenstackClient(o.config).UpdateOne(o)
}

// Unwrap unwraps the Openstack entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (o *Openstack) Unwrap() *Openstack {
	_tx, ok := o.config.driver.(*txDriver)
	if !ok {
		panic("ent: Openstack is not a transactional entity")
	}
	o.config.driver = _tx.drv
	return o
}

// String implements the fmt.Stringer.
func (o *Openstack) String() string {
	var builder strings.Builder
	builder.WriteString("Openstack(")
	builder.WriteString(fmt.Sprintf("id=%v, ", o.ID))
	builder.WriteString("timestamp=")
	builder.WriteString(o.Timestamp.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Openstacks is a parsable slice of Openstack.
type Openstacks []*Openstack
